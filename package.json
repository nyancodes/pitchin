{
  "name": "pitchin",
  "version": "1.0.0",
  "description": "",
  "main": "app.js",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/nyancodes/pitchin.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/nyancodes/pitchin/issues"
  },
  "homepage": "https://github.com/nyancodes/pitchin#readme",
  "scripts": {
    "start": "if-env NODE_ENV=production && npm run start:prod || npm run start:dev",
    "start:prod": "node server.js",
    "start:dev": "concurrently \"node server.js --ignore 'react/*'\" \"npm run react\"",
    "react": "cd react && npm run start",
    "install": "cd react && npm install",
    "build": "cd react && npm run build",
    "heroku-postbuild": "npm run build",
    "create-db": "psql -U postgres < schema.sql",
    "seed-db": "psql -U postgres < seed.sql"
  },
  "dependencies": {
    "axios": "^0.23.0",
    "bcrypt": "^5.0.1",
    "dotenv": "^10.0.0",
    "express": "^4.17.1",
    "passport": "^0.4.1",
    "passport-local": "^1.0.0",
    "pg": "^8.7.1"
  },
  "devDependencies": {
    "concurrently": "^4.1.0",
    "if-env": "^1.0.4"
  }
}
